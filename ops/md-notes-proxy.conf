# Include inside the Nginx Proxy Manager server block (server_proxy.conf).
# Only md-notes hosts are affected. TLS terminates at this proxy.
if ($host ~ ^((app\.)?mdnotes\.net|md\.mateo\.ovh)$) {
    set $x_forwarded_proto $scheme;
    set $x_forwarded_scheme $scheme;
}
